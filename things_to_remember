todo : 
	both android and python :
		big numbers, add space 
		switch from delay to speed, more natural
		speed percentage should be speeed_coefficient, initialized ad 1.0, step of like 0.1
		reading epub :
			' should concatenete before and after words
			: . , ? should concatenate only before, and add delay
			dont know for now "

	only android:
		bookmarks
		dark theme switch to light theme
		view mode
		options menu ? containing dark mode and sound (either read book + sound when turning page)
		history of last books read, and which page left
		fix information page, make it scrollable

	benchmark	

			
physical object using python :
	speaker for current word ? and/or page turn

	top:
		switch view - switch from word by word to full page

	left : 
		home
		on/off

	right:
		- speed
		+ speed

	bottom:
		go back 1 page
		go back 10 words
		pause
		go forward 10 words
		go forward 1 page
